# Avatar models

Drop `.vrm` (or `.glb`) files here and they appear in the console's avatar
picker, served from `/models/<filename>`.

The directory ships empty: no avatar is bundled, because none is licensed for
redistribution with this repository. With no file present the console offers the
Upload button instead of trying to load a default that does not exist.
